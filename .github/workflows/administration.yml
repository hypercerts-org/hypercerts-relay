name: Verify administration
on:
  pull_request:
    branches: [main]
    paths: ['administration/**', 'cmd/relay/**', 'jetstream/internal/hypercerts/**', '.github/workflows/administration.yml']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: jetstream/go.mod
          cache-dependency-path: |
            go.sum
            jetstream/go.sum
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: administration/package-lock.json
      - run: npm ci
        working-directory: administration
      - run: npm run check && npm test && npm run test:components && npm run build
        working-directory: administration
      - run: npx playwright install --with-deps chromium
        working-directory: administration
      - run: npm run test:browser
        working-directory: administration
      - run: git diff --check
