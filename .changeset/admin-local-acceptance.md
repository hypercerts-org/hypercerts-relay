---
'hypercerts-relay': minor
---

Adds local administration acceptance selectors for CSRF-protected, DID-authorized operations and accessible browser workflows against the private Relay and Jetstream owners. These use a documented test-only OAuth callback double and do not qualify a production OAuth provider.
